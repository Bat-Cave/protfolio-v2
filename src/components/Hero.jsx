const Hero = () => {
  return (
    <div className="relative w-screen h-screen">
      <div
        id="naker-destination"
        className="absolute w-screen h-[calc(100vh+4.75rem)] flex justify-center z-0 shadow-bottom-inner translate-y-[-4.75rem]"
      ></div>
    </div>
  );
};

export default Hero;
